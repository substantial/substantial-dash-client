<h2>{{title}}</h2>

{{yield}}

{{#each contents}}
  <div class="feed-event">
    <p>{{name}}</p>
    <p>{{#each datasets}} {{name}}: {{deal_count}} ({{formatted-as-dollars dollar_value}}) {{/each}}</p>
  </div>
{{else}}
  <p>No data</p>
{{/each}}
