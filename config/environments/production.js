// Put your production configuration here.
//
// This is useful when using a separate API
// endpoint in development than in production.
//
// window.ENV.public_key = '123456'

// Serve the client relatively, from the server's public directory
// (run `grunt server:dist`& copy dist/ into server)
window.ENV.api_base_url = '';
// ...or use a fully-qualified URL for the API.
// window.ENV.api_base_url = 'https://dash-api.my-amazing-company.com'

